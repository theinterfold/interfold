# Sizing fixtures only: the Prover.toml witnesses are up to ~30 MB each and are
# regenerated by `cargo run -p e3-zk-helpers --example gen_ckks_scale_provers -- --n <N>`.
# Package sources (Nargo.toml, src/) stay checked in so the shapes are reviewable.
*/Prover.toml
