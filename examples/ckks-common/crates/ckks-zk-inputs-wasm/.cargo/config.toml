# getrandom 0.3 wasm_js backend (see getrandom README "Opt-in backends").
# Mirrors crates/wasm/.cargo/config.toml.
[target.wasm32-unknown-unknown]
rustflags = ["--cfg", "getrandom_backend=\"wasm_js\""]
