---
name: update-flow-trace
description:
  Update the agent/ harness docs (flow-trace, invariants, context) to match the current branch's
  code changes. Use after you change contracts, circuits, actors, or CLI behavior.
---

Follow `agent/prompts/update-flow-trace.md` exactly. This file is a wrapper only. The canonical
procedure is in `agent/prompts/`.
