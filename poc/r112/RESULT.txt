# ROUND 112 — fold-seam pool (c2ab_fold / c3ab_fold / c4ab_fold / node_fold)
## source-minimality sweep + RAN re-anchor of the 4 load-bearing small fold gate counts
Date: 2026-09-06 (round 112). Box: 4c / 7.8 GiB + 8 GiB swap (unchanged; btime 1788243915, no
reboot, r89 protocol). Upstream: origin/main 499146c9 = merge-base, 0 commits since -> NO REBASE.
Working branch i5/dkg-research @ 2002634f (r111), ahead 55 / behind 0 vs origin/main.
Zero nargo/bb COMPILE (all 4 small fold artifacts read ON-DISK via `bb gates -t noir-recursive-no-zk`;
the durable small tree poc/r77/root was staged at r77 (2026-08-30), after all fold-cone source
commits, so no build was needed and no config was swapped).

## CLAIM (the r110 C3-lesson / r111 C2-lesson applied to the FOLD SEAM pool)
Every per-node LEAF circuit in the DKG DAG has had its cost structure source-minimality-swept:
C1 (r44/r34), C2a/C2b (r111), C3 (r110), C4 (I18/I19 r36/r37), C5 (I20 r38). The one family of
per-node circuits NEVER given the equivalent pass = the FOLD SEAM pool: c2ab_fold, c3ab_fold,
c4ab_fold, node_fold (the recursion that transits the leaf C1 proofs / chain-final proofs up to the
per-node fold and binds cross-seam consistency). r33's standing lesson (a fresh read of an
untouched region still surfaces a lever even when the queue is drained) + the r110/r111 pattern
(minimality sweep + re-anchor the load-bearing gate counts to a single re-runnable check) now
applied to this last family. Plus: are the 4 small fold gate counts (the load-bearing FOLD inputs
to the N=19 wall table's 73.5 s RAN-min-folds line) still digit-exact at HEAD, or have they drifted?

## METHOD (RAN, cheapest-first)
(1) SOURCE sweep of the whole fold-seam pool, file:line:
  - c2ab_fold/src/main.nr (53 L): verify_honk_proof(c2a) + verify_honk_proof(c2b) [ZK] +
    compute_vk_hash([c2a_kh,c2b_kh]) + unpack the two C2 public vectors into
    (c2a_expected, c2a_shares, c2b_expected, c2b_shares). ZERO sponge/challenge transcript.
  - c3ab_fold/src/main.nr (58 L): verify_honk_proof_non_zk(c3a chain-final) + _non_zk(c3b chain-final)
    + compute_vk_hash + unpack the two C3 chain-final publics into (pk_a,msg_a,ct_a,pk_b,msg_b,ct_b).
    ZERO sponge/transcript.
  - c4ab_fold/src/main.nr (53 L): verify_honk_proof(c4a) + verify_honk_proof(c4b) [ZK] +
    compute_vk_hash + unpack (c4a_expected, sk_agg, c4b_expected, esm_agg). ZERO sponge/transcript.
  - node_fold/src/main.nr (155 L): verify_honk_proof(c0) + _proof(c1) [ZK] + _non_zk(c2ab)+_non_zk(c3ab)
    +_non_zk(c4ab) + compute_vk_hash(5 kh) + the CROSS-SEAM SAME-CONSISTENCY asserts:
      * node_fold.nr:93-94  C1->C2 same-party:  c1_public[0]==c2a_expected_kh ; c1_public[2]==c2b_expected_kh
      * node_fold.nr:98-102 C3 cross-chain pk:  for every slot pk_a[j]==pk_b[j]
      * node_fold.nr:110-121 C2->C3 bind (non-self slot): c2a_shares[slot]==c3a_msg[slot] and
        c2b_shares[slot]==c3b_msg[slot]  (own slot excluded: bound via the C4 plaintext path instead).
    ZERO sponge/challenge transcript (the SF challenge sponge lives in the C3 LEAF, r110, not the fold).
  Grepped the whole pool for sponge|payload|challenge|compute_.*_challenge|fs_ => ZERO hits.
(2) RAN re-anchor: `bb gates -b <artifact> -t noir-recursive-no-zk` on the 4 ON-DISK small fold
    artifacts in the sha-pinned durable tree poc/r77/root/secure-8192/small/default/recursive_aggregation/
    (sha16 + digit cross-check vs the r21/r39/r46 small goldens). Re-runnable deliverable (tracked,
    in the interfold repo at interfold/poc/r112/): verify_fold_seam_anchor_r112.py (RC 0, SELF-CHECK OK).
(3) Cone-drift audit (r94 class): `git log` over the 4 seam-fold srcs + c3_fold. Per-file last touch:
    c2ab/c3ab/c4ab/node_fold = 2026-08-07 (nargo beta26 + bb 5.1.0 pin, commit 3c84684c);
    c3_fold = 2026-04-27. (The 2026-08-28 M7x commits r61/r62 touched a DIFFERENT circuit,
    c3_fold_batch_merge_m7x — not any of the 4 seam folds nor c3_fold.) The lib cone
    (bb_proof_verification + math/commitments) last touched 2026-08-20/22 (I14/I15) — BOTH
    only edited the C3 share_encryption LEAF, not the fold-consumed verify/commitment sub-cone.
    All 4 target srcs + the lib verify/commitment sub-cone STABLE since 2026-08-07, and the
    durable tree was staged 2026-08-30 (= r77) = AFTER all of that => 0 fold-cone drift to the
    digit for the nargo beta26 + bb 5.1.0 toolchain (same "0 cone drift" class as r110/r111).

## FINDINGS (RAN, exact)
(1) FOLD SEAM SOURCE-MINIMALITY = MINIMAL as written (the r110/r111-class question, RAN source-audited):
  All 4 folds are 2-3 independent RECURSIVE VERIFY seams over their children's final proofs + one-
  way vk-hash chaining (compute_vk_hash) + cross-seam UF consistency asserts. They carry NO
  sponge/challenge transcript (grepped: zero hits) and NO duplication of raw witness elements
  (the only bytes carried are the children's PUBLIC signals + the fixed-dimension UltraHonk
  {ZKProof,VerificationKey} witnesses, which are consumed by the ->onchain transitive-enforcement
  chain and cannot be deduped without breaking it, by the exact I5a "remove any verify -> trusting-
  the-prover violation" argument). Therefore the I3/I14 redundant-raw-push class is STRUCTURALLY
  ABSENT in the fold pool (as it was in C2, r111) AND no new box-1 fold lever exists:
    - dropping any verify_honk_proof[_non_zk] => that child's ZK validity becomes unenforced by the
      final onchain-verified node_fold (transitive enforcement breaks) = trust-the-prover; DEAD.
    - the node_fold 3 cross-seam asserts (C1->C2 :93-94, C3 pk_a==pk_b :98-102, C2->C3 :110-121) are the
      ONLY inter-circuit soundness link (each binds a distinct circuit pair's publics; none redundant).
    - compute_vk_hash = a 1-way commitment that bindings the vk set for the DkgFoldAttestationVerifier
      onchain pin; dropping it breaks the ->onchain vk binding; DEAD.
  => the fold pool is source-MINIMAL; the per-node minimality question for the WHOLE DKG DAG
  (C1..C5 leaves + c3 kernel/leaf + 4 seam folds) is now RAN-CLOSED box-1.
(2) 4 LOAD-BEARING FOLD GATE COUNTS RAN re-anchored (single-sourced + re-runnable, DIGIT-EXACT to
  every prior baseline, 0 fold-cone drift since 3c84684c 2026-08-07 toolchain pin), secure-8192/SMALL
  (N=19/T=9/H=10/L=3), `bb gates -t noir-recursive-no-zk` on the poc/r77/root durable tree:
    - c2ab_fold  = 1,473,584 gates / 122 ACIR  (sha16 a46ddf7f4dc494bb) DIGIT-EXACT r21/r39
    - c3ab_fold  = 1,437,844 gates / 348 ACIR  (sha16 ad5736fc4c082c4a) DIGIT-EXACT r21/r39
    - c4ab_fold  = 1,471,783 gates / 68 ACIR   (sha256ed 2cd4411a80008822) DIGIT-EXACT r21/r39
    - node_fold  = 3,719,958 gates / 438 ACIR  (sha16 6a50a30e55eb9ae9) DIGIT-EXACT r21/r39
  These are the FOLD inputs the N=19 wall table reads (the "c2ab+c4ab+node 73.5 s [RAN min floor]"
  line, r76 — the wall is proven on the min box-1 legs and the small arms come from box-2 by
  construction). The N=19 header 90.1 min/node @4c EXCL. comm is UNTOUCHED (no new node wall).

## VERDICT (RAN, box-1)
I-r112 = CLOSED RAN (negative + anchor), the fold analogue of r110 (C3) / r111 (C2). (a) The
fold-seam box-1 lever question is RAN-CLOSED negative: the 4 folds are source-MINIMAL (every
verify-seam + every cross-seam assert + the vk-hash is load-bearing; the I3/I14 redundant-raw-push
class is structurally absent because there is no sponge transcript to de-dup); no new box-1 fold
lever. (b) The model's 4 load-bearing small fold gate counts are RAN re-anchored to a single
re-runnable check, all DIGIT-EXACT to the r21/r39/r46 goldens with 0 cone drift. Per-node-minimal
-ity for the ENTIRE DKG DAG (C1..C5 + c3 + 4 seam folds) is now RAN-closed on box-1. The ONE forward
path is unchanged: box-2 (owner directive 2026-09-05 — 4c/7.8 GiB is max here). No repo source
change (out-of-repo anchor under poc/r112/; gate N/A, no commit).
